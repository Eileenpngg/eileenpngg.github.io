source "https://rubygems.org"

# github-pages bundles Jekyll with every plugin and version GitHub Pages supports.
# Using this gem ensures local builds match the live site exactly.
gem "github-pages", group: :jekyll_plugins

gem "jekyll-seo-tag"
